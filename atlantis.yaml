version: 3
projects:
- name: production
  dir: production
  workspace: default
  autoplan:
    when_modified: ["*.tf", "../modules/**/*.tf"]
    enabled: false
- name: staging
  dir: test-server/staging
  workspace: default
  autoplan:
    when_modified: ["**/*.tf"]
    enabled: true