version: 3
projects:
- name: production
  dir: production
  workspace: default
  autoplan:
    when_modified: ["*.tf", "../modules/**/*.tf"]
    enabled: false
- name: staging
  dir: atlantis-example/test-server/staging/*
  workspace: default
  autoplan:
    when_modified: ["*.tf", "../modules/**/*.tf"]
    enabled: true